title: Tatu Arvela
email: contact@tatuarvela.com
baseurl: '/' # the subpath of your site, e.g. /blog
url: '' # the base hostname & protocol for your site, e.g. http://example.com
permalink: pretty

defaults:
  - scope:
      path: '*'
    values:
      show_content_header: true

# Build settings
markdown: kramdown
sass:
  sass_dir: _scss
